declare module "cal-heatmap/plugins/Tooltip" {
  import type { Plugin } from "cal-heatmap";
  const Tooltip: Plugin;
  export default Tooltip;
}

declare module "cal-heatmap/plugins/Legend" {
  import type { Plugin } from "cal-heatmap";
  const Legend: Plugin;
  export default Legend;
}
